dependencies:
  pre:
    - npm install -g grunt
    - npm install -g grunt-cli

test:
  override:
    - grunt circletest
  post:
    - mkdir -p $CIRCLE_TEST_REPORTS/junit/
    - find . -type f -regex ".*/junitreport/.*xml" -exec cp {} $CIRCLE_TEST_REPORTS/junit/ \;
    
